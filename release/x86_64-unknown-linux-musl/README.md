# Sunbay SoftPOS Backend - Linux Release

## Target Platform
- Architecture: x86_64-unknown-linux-musl
- Build Date: 2025年12月 2日 星期二 14时47分46秒 CST

## Installation

1. Extract the archive
2. Configure environment variables in `.env`
3. Run the binary:
   ```bash
   ./sunbay-softpos-backend
   ```

## Configuration

Edit the `.env` file and `config/production.yaml` to match your environment.

## Requirements

- Linux x86_64 (statically linked, no dependencies)
